# Production Environment Configuration for Backend
# Copy this file to .env.production and fill in your real values

# Server Configuration
PORT=3001
NODE_ENV=production

# Database Configuration - PostgreSQL
# Replace with your actual database credentials from hardweb.ro hosting
DB_HOST=your-database-host.hardweb.ro
DB_PORT=5432
DB_NAME=kids_athletics
DB_USER=your_database_user
DB_PASSWORD=your_secure_database_password

# JWT Configuration
# Generate a strong random key: openssl rand -hex 32
# IMPORTANT: Change this for production!
JWT_SECRET=generate-a-strong-random-32-character-hex-string-here-minimum-32-chars
JWT_EXPIRES_IN=7d

# Frontend URL (for CORS)
# Set to your production domain
FRONTEND_URL=https://hardweb.ro

# API URL
# Set to your production API URL
API_URL=https://api.hardweb.ro

# Optional: Enable logging
# LOG_LEVEL=info

# Optional: Email Configuration (if you add email features later)
# SMTP_HOST=smtp.your-email-provider.com
# SMTP_PORT=587
# SMTP_USER=your-email@domain.com
# SMTP_PASSWORD=your-email-password
# SMTP_FROM=noreply@hardweb.ro

# Optional: Enable debug mode (ONLY for development!)
# DEBUG=false
