# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist-ssr
*-dist
*.local

# Keep entry HTML tracked so Vite can build on any environment
# (dist/index.html is generated at build time and may overwrite this during deploy)
# Previously this was ignored which broke server builds due to missing entry.

# Prevent stray built assets at repo root (we only keep dist/)
/index-*.js
/index-*.css
/react-vendors-*.js
/ui-vendors-*.js
/chart-vendors-*.js
/icons-*.js
/utils-*.js
/dist.tar.gz

# Avoid accidental duplicate builds inside src/
/src/dist/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.env
**/agent-eval-report*
packages
pids
.file-manifest
.devcontainer/

.spark-workbench-id

# Server
server/node_modules
server/.env
